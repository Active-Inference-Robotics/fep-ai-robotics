[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "fep_ai_robotics"
version = "0.1.0"
description = "Main package for FEP AI Robotics."
authors = ["Your Name <your.email@example.com>"]
readme = "README.md"
requires-python = ">=3.8"
dependencies = [
    "numpy>=1.21",
    "scipy>=1.7"
    # Add other main package dependencies here
]

[tool.uv]
# This section can be used for uv-specific settings if needed
